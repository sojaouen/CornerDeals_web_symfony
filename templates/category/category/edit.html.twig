{% extends 'base.html.twig' %}

{# Page Settings #}
{% set title = "Modification de la catégorie : " ~ category.name %}


{# Page Title #}
{% block title %}{{ title }} - {{ parent() }}{% endblock %}


{# Page Content #}
{% block body %}
<div class="container">
    <div class="col-lg-8 offset-lg-2">

    {# Header / Titre de la page #}
    <header class="page-header">
        <h1 class="page-title text-center py-5">{{ title }}</h1>
    </header>

    {{ form_start( form ) }}
    {{ form_widget( form ) }}
    {{ form_end( form ) }}

    <button type="submit" class="btn btn-primary col-12 my-4 py-3">Modifier</button><br>

    <a href="{{ path('category:index') }}">Retour</a>


     </div>
</div>
{% endblock %}
