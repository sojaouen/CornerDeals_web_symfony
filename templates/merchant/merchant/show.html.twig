{% extends 'base.html.twig' %}

{% block title %}Merchant{% endblock %}

{% block body %}
    <h1>Merchant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ merchant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ merchant.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ merchant.description }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ merchant.website }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('merchant:index') }}">back to list</a>

    <a href="{{ path('merchant:edit', {'id': merchant.id}) }}">edit</a>

    {# TODO: Add Deals list #}
    {# TODO: Add DiscountCode list #}

    {{ include('merchant/merchant/_delete_form.html.twig') }}
{% endblock %}
