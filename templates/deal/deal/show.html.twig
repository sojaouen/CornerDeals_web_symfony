{% extends 'base.html.twig' %}

{% block title %}Deal{% endblock %}

{% block body %}
    <h1>Deal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deal.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ deal.title }}</td>
            </tr>
            <tr>
                <th>CrossedOutPrice</th>
                <td>{{ deal.crossedOutPrice }}</td>
            </tr>
            <tr>
                <th>DealPrice</th>
                <td>{{ deal.dealPrice }}</td>
            </tr>
            <tr>
                <th>Discount</th>
                <td>{{ deal.discount }}</td>
            </tr>
            <tr>
                <th>DiscountUnity</th>
                <td>{{ deal.discountUnity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('deal:index') }}">back to list</a>

    <a href="{{ path('deal:edit', {'id': deal.id}) }}">edit</a>

    {{ include('deal/deal/_delete_form.html.twig') }}
{% endblock %}
