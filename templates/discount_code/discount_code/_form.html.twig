{{ form_start(form, {
    attr: {
        novalidate: "novalidate"
    }
}) }}

    {{ form_widget(form) }}
    <button type="submit">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
