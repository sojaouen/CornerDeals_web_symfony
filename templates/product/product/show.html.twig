{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product:index') }}">back to list</a>

    <a href="{{ path('product:edit', {'id': product.id}) }}">edit</a>

    {# TODO: Show Deals of this product #}
    {# TODO: Show DiscountCode of this product #}
    {# TODO: Show Gallery of this product #}

    {{ include('product/product/_delete_form.html.twig') }}
{% endblock %}
